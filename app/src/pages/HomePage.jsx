
import React, { Fragment } from "react";

const HomePage = () => (
    <Fragment>
        <h2>Welcome!</h2>
        <p>Click in login button to access application.</p>
    </Fragment>
);

export default HomePage;